<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Garama Prenotazioni</title>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="icon" href="/img/logo.jpg">
</head>
<body>
    <img id="backg" src="/img/shaw.jpeg" alt="Background">
    <div class="nav">
        <div class="block">
            <img src="/img/logo.jpg" alt="Logo" width="50" height="50" onclick="location.href='/'">
            <h2>Garama</h2>
        </div>
        <% if(!locals.logged){%>
            <a href="/login">Login\Register</a>
        <%}else{%>
            <a href="/dash">Logged - <%=locals.logged%></a>
        <%}%>
    </div>
    <div class="cont">
        <%if(!locals.luogo){%>
            <h1>Nessun elemento trovato</h1>
        <%} else{%>
            <form method="post" action="/prenotaposto">
                <div class="row">
                    <input name="data" placeholder="Data prenotazione" type="date">
                </div>
                <div class="row">
                    <input name="numero" placeholder="N persone" type="number" min="1">
                </div>
                <input type="hidden" name="luogo" value="<%=locals.luogo.id%>">
                <input type="submit" value="Conferma">
            </form>
        <%}%>
    </div>
    <div class="down">
        <h5>Shaw - Copyright° Hornet 2025</h5>
    </div>
</body>
</html>